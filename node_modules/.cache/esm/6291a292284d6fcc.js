_284‍.x([["findAll",()=>findAll],["findOne",()=>findOne],["store",()=>store],["update",()=>update],["remove",()=>remove]]);const data = []

//listar
       const findAll = () =>{
    return data;
};

//=== compara tanto el tipo y valor
//== compara solo el valor
//buscar por id
       const findOne = (id) =>{
    return data.find((u)=> u.id === Number(id));
};

//crear
       const store = (user) => {
    user.id = data.length + 1;
    data.push(
        user
    );
}

//update
       const update = (id, user) => {
    const index = data.findIndex((u)=> u.id === Number(id));
    data[index] = {
        //antiguo
        ...data[index],
        //chancamos al antiguo con la nueva data
        ...user
    }
}

//remove
//data.length = 0; limpiar el array
       const remove = (id) => {
    const users = data.filter((u)=> u.id !== Number(id));
    data.length = 0;
    data.push(...users);
}
